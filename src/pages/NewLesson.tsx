import React, { useRef } from "react";
import "./step.css";

interface props {
  activeLvl: number;
}

const Step: React.FC<props> = ({ activeLvl }) => {
  return <div className="main_body"></div>;
};

export default Step;
